# Portfolio Project — Codex Workspace

## Branch: codex-design
This worktree is for Claude's development work. Changes here stay on `codex-design` and are never pushed directly to `main`.

## Rules
- Do not restructure existing files or folders
- Do not overwrite working code without explicit instruction
- Keep all changes scoped to `codex-design` branch
- When done with a feature, summarize changes so they can be reviewed before merging to `main`

## Repo Structure
- `index.html` — main portfolio page
- `css/` — stylesheets
- `js/` — scripts
- `games/` — browser-based games (flappy-bird, guess-character)
- `projects/` — project showcase pages
- `resume.pdf` — linked resume

## Merge Strategy
Changes merge into `main` only after review. Coordinate with `codex-design` branch before merging either branch to avoid conflicts.
